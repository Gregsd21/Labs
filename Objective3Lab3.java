   public class Objective3Lab3 {
     public static void main(String[] args) {
       int first, second, third;
       first= 1 + (int)(Math.random() * 6);
       second=1 + (int)(Math.random() * 6);
       third=1 + (int)(Math.random() * 6);
       int sum= first+second+third;
       System.out.println(sum);

       /* I checked my work by adding a 1 where all three 6's were. Then I changed
       one of the ones to 2, to double check as all ones only could give an answer
       of 3. */
     }
   }
